module.exports = {
  "transpileDependencies": [
    "vuetify"
  ],

  // publicPath: process.env.NODE_ENV === 'production' ? '/MANTENIMIENTO/' : '/',
  publicPath: '',


  pluginOptions: {
    cordovaPath: 'src-cordova'
  }
}
